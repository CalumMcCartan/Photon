func int main()
{
  #whatever
  image img;
  int r;

  int index;

  
  img = load("sky.jpg");

  prints("testing get_pixel");
  
  for( index = 0; index < 3; index = index+1) {
    #r = index;
    r = get_pixel(img, index);
    print(r);
  }

  return 0;
}
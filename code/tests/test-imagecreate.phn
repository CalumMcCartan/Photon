func int main()
{
  Image img;
  int wid;
  int ht;

  pint r;
  pint g;
  pint b;
  pint a;

  r = g = 0;
  b = a = 255;
  img = create(800, 600, r, g, b, a);

  wid = width(img);
  ht = height(img);
  print(wid);
  print(ht);

  save(img, "blueImgTest.png");

  return 0;
}
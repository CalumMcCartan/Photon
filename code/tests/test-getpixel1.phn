func int main()
{
  #whatever
  image img;
  
  int index;
  int p;
  int x;
  int y;
  int position;
  int channels;
  int[] ps;

  
  img = load("sky.jpg");

  #for gettign a specific pixel, input what you want
  x = 1;
  y = 0;

  #array math, assumes 4 channels
  channels = 4;
  position = (x + (width(img) * y) ) * channels;  

  for( index = position; index < position + 4; index = index+1 ) {
    p = get_pixel(img, index);
    array_add(ps,p);
    print(p);
    img = load("sky.jpg");
  }

  for ( index = 0; index < ps.length; index = index+1) {
    print(ps[index]);
  }

  return 0;
}
func int main()
{
  float img;
  int wid;
  int ht;

  #load non existed image should returns error
  img = load("Shapes.png");

  wid = width(img);
  ht = height(img);
  print(wid);
  print(ht);

  save(img, "ShapesSaved.png");

  return 0;
}